---
'@jirawatpyk/aura-react': patch
'@jirawatpyk/aura-tokens': patch
---

SideNav: long labels wrap to a second line instead of an ellipsis (Chamber-OS item 63). One-line rows keep 36px (44px on touch), a two-line row grows to 44px, labels clamp past two lines, and the collapsed rail is unchanged.
