import { Meta, ArgTypes } from '@storybook/blocks';
import * as Aura from '@aura/react';

<Meta title="Props/Theming and locale" />

# Theming and locale — props

A brand colour per project, and Thai or English built-in labels. Tables are generated from the TypeScript sources of `@jirawatpyk/aura-react`, so they always match the published types. `*` = required.

## ThemeStyle

<ArgTypes of={Aura.ThemeStyle} />

## AuraProvider

<ArgTypes of={Aura.AuraProvider} />
## ColorSchemeToggle

<ArgTypes of={Aura.ColorSchemeToggle} />

## ColorSchemeScript

Put it in `<head>`: it applies the saved choice (or `system`) before the page paints, so there is no flash of the wrong scheme. `useColorScheme()` returns `{ scheme, resolved, setScheme }` for your own controls.

<ArgTypes of={Aura.ColorSchemeScript} />
