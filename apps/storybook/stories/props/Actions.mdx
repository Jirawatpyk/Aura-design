import { Meta, ArgTypes } from '@storybook/blocks';
import * as Aura from '@aura/react';

<Meta title="Props/Actions" />

# Actions — props

Buttons and menus. Tables are generated from the TypeScript sources of `@jirawatpyk/aura-react`, so they always match the published types. `*` = required.

## Button

<ArgTypes of={Aura.Button} />

## IconButton

<ArgTypes of={Aura.IconButton} />

## Menu

<ArgTypes of={Aura.Menu} />

## DropdownMenu

<ArgTypes of={Aura.DropdownMenu} />

## Tag

<ArgTypes of={Aura.Tag} />
