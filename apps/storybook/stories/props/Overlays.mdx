import { Meta, ArgTypes } from '@storybook/blocks';
import * as Aura from '@aura/react';

<Meta title="Props/Overlays" />

# Overlays — props

Modal (focus trap, scroll lock, focus restore) for Dialog and Drawer; Popover is non-modal. Tables are generated from the TypeScript sources of `@jirawatpyk/aura-react`, so they always match the published types. `*` = required.

## Dialog

<ArgTypes of={Aura.Dialog} />

## Drawer

<ArgTypes of={Aura.Drawer} />

## Popover

<ArgTypes of={Aura.Popover} />
